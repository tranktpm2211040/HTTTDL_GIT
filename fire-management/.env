# Copy to .env and fill values
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=chay_rung
# Example table schema (run in MySQL to create table):
#
# CREATE DATABASE fire_management;
# USE fire_management;
# CREATE TABLE fires (
#   id INT AUTO_INCREMENT PRIMARY KEY,
#   name VARCHAR(255) NOT NULL,
#   lat DOUBLE NOT NULL,
#   lng DOUBLE NOT NULL,
#   discoveryTime DATETIME NOT NULL,
#   dangerLevel VARCHAR(50),
#   affectedArea DOUBLE,
#   province VARCHAR(100),
#   status VARCHAR(50),
#   district VARCHAR(100),
#   cause TEXT,
#   newsUrl TEXT
# );
